{
  "name": "qr-code-generator",
  "version": "1.0.0",
  "description": "A simple QR code generator website",
  "main": "index.html",
  "scripts": {
    "start": "live-server",  // Run this for local development using `npm start`
    "build": "echo 'No build step required'"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.1"  // Optional, for running locally
  }
}
